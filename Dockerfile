# Используем официальный образ Python
FROM python:3.9-slim

# Установим рабочую директорию в контейнере
WORKDIR /app

# Копируем наш серверный код в контейнер
COPY server.py .

# Открываем порт 8080 для внешнего доступа
EXPOSE 8080

# Устанавливаем команду для запуска HTTP-сервера
CMD ["python", "server.py"]
